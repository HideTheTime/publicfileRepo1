name: Build

on:
  push:
    branches: ["*"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.x"

      - env:
          NB1: ${{ secrets.NB1 }}
        run: echo $NB1

      # - name: Install dependencies
      #   run: npm install

      # - name: Build project
      #   run: npm run build
