name: Build

on:
  push:
    branches: ['*']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.x'

      - name: learn workflows
        run: echo Hello build.yaml
      - run: node -v
      - run: ${{nb1}}

      # - name: Install dependencies
      #   run: npm install

      # - name: Build project
      #   run: npm run build
